# Milevia — 协作规则

本项目的协作规则以 `AGENTS.md` 为准，请先阅读它。

- **发布 / 发版 / 部署 / 上线**：先读 `docs/35-发布操作手册.md`，里面有准确的命令、前置条件与验证步骤。
- **未经用户明确要求，不执行任何 `git add` / `commit` / `push` 或创建 PR。**
- 服务器空间紧张，每个平台只保留最新一份安装包。
