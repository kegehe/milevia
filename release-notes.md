# Milevia v0.1.0

首次对外发布。Milevia 是一个**多项目 AI 自动开发平台**，统一管理本地与远程项目，通过 Claude Code / Codex CLI 进行持续的 AI 开发对话。

## ✨ 核心能力

- **多项目管理** — 同时加载 Windows 本地、WSL 发行版、SSH 远程服务器上的项目，仪表盘实时显示运行状态
- **AI 对话闭环** — 每个项目独立的 Claude Code / Codex 会话，支持会话恢复、历史搜索与连续对话
- **实时过程展示** — 时间线呈现 AI 消息、工具调用、命令输出与审批请求，执行期间可继续输入
- **双权限模式** — 默认需人工确认命令；「完全控制」模式允许 AI 直接执行
- **任务编排** — 可视化任务看板，创建、下发、跟踪开发任务，串联对话与代码变更
- **文件浏览与编辑** — 内置 CodeMirror 编辑器，文件树浏览
- **Git 工作台** — 查看变更、暂存、提交，与 AI 修改衔接
- **跨项目通知** — 统一通知中心聚合各项目的运行状态与事件
- **Windows 桌面端** — Tauri 2 原生壳，系统托盘驻留、单实例管理、自动拉起本地控制服务
- **会话安全** — 桌面端每次启动生成独立会话令牌，前端与 sidecar 经随机端口 + 令牌通信

## 🖥 端

- Windows 桌面应用（Tauri 2 / WebView2）
- Web 浏览器版（共用同一前端代码）

## 🧰 安装与升级

- **安装**：下载 `Milevia_0.1.0_x64-setup.exe` 运行即可；安装包会自动引导 WebView2 运行时（必要时联网下载）。
- **在线升级**：安装后启动时自动检查新版本，有新版时在主界面右上角提示，点击「立即升级」即可完成更新。

## 📦 本次发布文件

| 文件 | 说明 |
|---|---|
| `Milevia_0.1.0_x64-setup.exe` | NSIS 安装包（推荐，用于安装与在线升级） |
| `Milevia_0.1.0_x64_en-US.msi` | MSI 安装包 |
| `latest.json` | 在线升级清单（配合桌面端 updater 使用） |

## ⚠️ 说明

- 本版本为**首个对外基线**，Web 与桌面共用同一套代码。
- 桌面端需本机具备 Claude Code 或 Codex CLI，方可进行 AI 对话。
