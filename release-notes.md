# Milevia v0.1.5

本版本重点解决移动端远程控制在实际使用中无法完成连接与配对的问题，并提升长时间运行的稳定性与移动端同步效率。

## 重点更新

### 移动端与桌面端连接

- 修复桌面端无法生成配对二维码的问题：远程控制页此前会因为权限校验不通过而无法创建配对。
- 配对现在必须经电脑端确认后才生效，扫码本身不再直接获得访问权限。
- 新增远程服务状态提示：电脑端 Agent 尚未注册时，页面会引导粘贴部署注册令牌完成一次注册，并在注册完成后自动恢复可用状态。
- 撤销访问改为只解除手机绑定，不再连带把电脑端下线。

### 指令下发与执行

- 电脑离线时不再接受下发指令，改为明确提示；此前指令会被接受但实际无人执行。
- 正在执行的任务不再被误判为「已过期」，只有从未投递的指令会因超时失效。
- 指令在电脑端中断后可以自动恢复并回报真实结果，不再长期停留在「处理中」。
- 修复错误提示与场景不符的问题，例如把「电脑离线」提示成「配对码已被使用」。

### 同步与流量

- 内容未变化时不再重复传输完整快照，电脑端也会合并短时间内的多次上传，明显降低手机流量。
- 修复电脑端已删除的消息在手机上重新出现的问题。

### 稳定性

- 修复事件序号冲突导致电脑端反复断线重连、后续事件无法送达的问题。
- 长时间运行下的凭据刷新与重连更稳健；远程指令执行增加了超时保护。

## 安装与升级

- Windows：下载 `Milevia_0.1.5_x64-setup.exe` 并运行；已安装旧版本的用户可通过应用内更新升级。
- Android：下载 `Milevia_0.1.5_android.apk` 安装。首次安装或从非应用商店安装时，请按系统提示允许安装未知来源应用。

## 发布文件

| 文件 | 说明 |
|---|---|
| `Milevia_0.1.5_x64-setup.exe` | Windows NSIS 安装包，推荐；也是在线升级使用的签名更新包 |
| `Milevia_0.1.5_x64_zh-CN.msi` | Windows MSI 安装包 |
| `Milevia_0.1.5_android.apk` | Android 移动端安装包 |
| `latest.json` | Windows 在线升级清单 |

## 说明

- 移动端远程控制需要电脑端 Agent 在线并完成一次配对。
- 升级后如果电脑端此前从未注册过远程服务，请在「远程控制」页面按提示完成一次注册（注册令牌只使用一次，注册成功后凭据保存在本机）。
- 当前 Android APK 使用本机 debug 签名，适用于直接安装和内测；正式上架 Google Play 前需替换为长期保管的发布 keystore。
