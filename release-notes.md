# Milevia v0.1.6

> 桌面端 **v0.1.6**；手机端本次为 **v0.1.7**（Android 版本号与桌面端各自递增，见文末说明）。

本版本主要有两条线：一是让手机上看电脑的体验跟上电脑上的操作（响应快、逐字回复、界面重做），
二是补齐会话级模型选择、常用命令与 MCP 连接向导。

## 重点更新

### 会话与命令

- 底部模型栏从只读改为可选：每个会话可以单独指定模型，优先级为「会话选择 > 档案模型 > CLI 默认」，
  本机、WSL、SSH 三条执行路径统一生效，切换后下一条消息即按新模型执行。
- 常用命令目录化：命令列表取自 CLI 自己报告的命令集，新增命令选择器；在输入框敲 `/` 即可补全。

### MCP 连接

- MCP 管理页重做为一键连接向导：内置预置目录，两步完成接入；新增「草稿试连」与「运行时检查」，
  可以配好确认能连通再启用，不必先保存再试错。

### 项目编排

- 新增发布快照：把开发分支当前提交固定成一个不可变分支（`release/<sha>`）。确认发布时会先校验
  稳定分支确实包含该快照，再逐个校验快照包含各任务的集成提交，避免「发出去的不是验证过的那份代码」。

### 响应速度与稳定性

- 项目列表刷新与连通性探测解耦，前端并发刷新请求做合并 —— 修掉事件密集时连接池被占满、
  随后点开项目要等 15 秒超时的问题。
- WSL 探测改为「先返回旧值、后台刷新」并合并并发请求，冷启动不再让项目页接口稳定卡 5 秒；
  新增 WSL 保活，避免发行版空闲休眠后每次调用都付一次冷启动代价。
- 事件表按会话设置保留上限，并新增数据库空间回收：日常只做采样估算，真正清理时才扫全表并回收已释放的页。
- 标题与报错文本按字符边界截断，中文不再被切成半个字。
- 桌面端升级时不再弹「无法打开要写入的文件: …\milevia-control.exe」：安装前后会自动结束随包的四个后台进程。

### 手机端

- **明显更快**：收到助手消息后不再重新拉取整份项目与会话快照（此前快照体积随项目数与历史消息数线性增长），
  改为事件驱动；助手回复支持逐字显示。
- 全新的品牌启动页与自适应图标，App 启动全程不再闪白。
- 远程页重做：补上运行过程（重试 / 压缩上下文 / 后台任务 / 执行失败）、工具确认只读卡、
  常用命令与技能可选；修掉扫码取景画面被背景盖住的问题。
- 项目列表卡片重做。
- 物理返回键改为分层回退，最外层才最小化应用（此前按返回键没有反应）。
- 关闭系统自动备份，避免云端访问令牌随备份外泄。
- 顶部安全区适配、消息列表贴底跟随、页面不可见时暂停轮询、触控目标补足到 44px、小字号统一到 12px。

### 电脑端与云端连接

- 电脑端改为事件驱动的长轮询唤醒，去掉固定轮询周期带来的延迟；助手回复的增量文本随事件下发，
  手机端因此能逐字显示。
- 修复云端事件流停滞：排空查询取不到最新事件，导致手机端长时间收不到新内容。

## 安装与升级

- **Windows**：下载 `Milevia_0.1.6_x64-setup.exe` 运行；已安装旧版本的用户可在应用内检查更新。
- **Android**：下载 `Milevia_0.1.7_android.apk` 安装；已装 0.1.6 的手机会在 App 内看到
  「发现新版本」提示，点「立即更新」即可。
- 手机端与桌面端版本号各自递增，因此本次两端版本号不同（桌面 0.1.6 / 手机 0.1.7），不影响配对与连接。

## 发布文件

| 文件 | 说明 |
|---|---|
| `Milevia_0.1.6_x64-setup.exe` | Windows NSIS 安装包，推荐；也是在线升级使用的签名更新包 |
| `Milevia_0.1.7_android.apk` | Android 移动端安装包 |
| `latest.json` | 在线升级清单，桌面端与手机端共同读取 |

## 说明

- 移动端远程控制需要电脑端在线，并完成过一次配对。
- 升级后如果电脑端此前从未注册过远程服务，请在「远程控制」页面按提示完成一次注册
  （注册令牌只使用一次，注册成功后凭据保存在本机）。
- 当前 Android APK 使用本机 debug 签名，适用于直接安装和内测；正式上架应用商店前需替换为长期保管的发布 keystore。
