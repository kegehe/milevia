# Milevia v0.1.2

本版本强化了多项目 AI 开发的任务编排、运行环境与对话请求可靠性。

## 重点更新

### 对话请求可靠性

- 对话发送加入客户端请求 ID 与服务端持久化幂等保护。
- 网络超时或暂时不可用后的同一次自动重试会复用原请求，不会重复创建消息或 AI 运行实例。
- 同一请求 ID 但内容不一致会被拒绝，避免错误复用。
- 保留针对上游临时不可用的重试能力。

### 任务编排与队列

- 完善任务看板、队列及编排工作区，集中查看任务状态、运行过程和后续操作。
- 改进任务恢复、停止、执行状态同步与并发场景下的工作目录保护。
- 增强运行时间线和输出中的链接识别，便于定位执行结果。

### 运行环境与连接

- 改进本地、SSH 与 WSL 环境中的 Claude Code / Codex CLI 探测、执行和错误处理。
- 强化 SSH 连接、项目运行与 CLI 更新流程的稳定性。
- 改进桌面端托盘与窗口状态管理。

### 优化建议

- 优化建议工作区支持更完整的扫描、筛选、编辑、删除和发送到对话流程。
- 改进建议去重、验证与汇总结果，减少重复或遗漏的建议。

## 安装与升级

- 新安装：下载 `Milevia_0.1.2_x64-setup.exe` 并运行。
- 在线升级：已安装旧版本的用户启动应用后可检测并升级到 0.1.2。

## 发布文件

| 文件 | 说明 |
|---|---|
| `Milevia_0.1.2_x64-setup.exe` | NSIS 安装包，推荐；也是在线升级使用的签名更新包 |
| `Milevia_0.1.2_x64_zh-CN.msi` | MSI 安装包 |
| `latest.json` | 在线升级清单 |

## 说明

- 本版本的幂等保护覆盖带相同客户端请求 ID 的重复提交。旧客户端或用户主动重新发送会被视为新的请求。
- 尚未引入跨不同请求的全局 RPM、TPM 或 token 预算限制。
