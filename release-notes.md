# Milevia v0.1.3

本版本围绕多项目对话工作区、终端执行和项目文件工具链进行完善，提升长时间运行、并发操作和 Windows 桌面端使用的稳定性。

## 重点更新

### 多项目对话工作区

- 新增项目级多会话 Tab，支持会话切换、关闭、未读状态和最近活动定位。
- 对话会话支持独立工作区与生命周期管理，工具调用、文件操作、Git 操作和项目运行均使用服务端确认的会话工作区。
- 增强会话恢复、请求幂等保护和并发场景下的工作目录隔离，降低重复提交和跨会话误操作风险。

### 终端与运行环境

- 改进 Windows 终端桥接、进程树处理、启动参数和终端窗口生命周期管理。
- 优化本地、WSL 与 SSH 环境中的 CLI 探测、连接、执行和错误处理。
- 完善项目运行、暂停/恢复、停止和资源清理流程，增强工作区占用保护。

### 文件、Git 与数据查看

- 扩展文件预览与编辑能力，改进多格式文件、SQLite 数据库和项目文件树的交互。
- 优化 Git 工作台的状态展示、变更操作和会话工作区适配。
- 改进运行输出、时间线和链接识别，便于定位命令与执行结果。

### 任务编排与优化建议

- 完善任务队列、编排状态同步、依赖关系和合并/清理操作。
- 优化建议支持更完整的扫描、筛选、编辑、删除和发送到对话流程。
- 增加请求重试、竞态防护和边界场景测试，提升前后端一致性。

## 安装与升级

- 新安装：下载 `Milevia_0.1.3_x64-setup.exe` 并运行。
- 在线升级：已安装旧版本的用户启动应用后可检测并升级到 0.1.3。

## 发布文件

| 文件 | 说明 |
|---|---|
| `Milevia_0.1.3_x64-setup.exe` | NSIS 安装包，推荐；也是在线升级使用的签名更新包 |
| `Milevia_0.1.3_x64_zh-CN.msi` | MSI 安装包 |
| `latest.json` | 在线升级清单 |

## 说明

- 会话工作区会在服务端校验项目归属和工作区状态后用于文件、Git、终端及运行操作。
- 旧客户端仍可使用项目共享工作区；升级后新建会话可使用独立工作区。
