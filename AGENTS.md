# 项目协作规则

## 语言

- 始终使用简洁的中文回答，代码、术语、命令除外。

## Git 操作

- 除非用户在当前对话中明确要求，禁止执行 `git add`、`git commit`、`git push` 或创建 Pull Request。
- 代码、测试、文档和配置的修改都应保留在工作区，供用户自行审阅和提交。
- 完成实现和验证后，只报告变更与验证结果；不得将“任务完成”视为提交授权。

## 发布

用户要求「发布 / 发版 / 部署 / 上线 / 传个新版本」时，**先读 `docs/35-发布操作手册.md` 再动手**，
不要凭记忆猜命令。手册里有准确命令、前置条件、验证方式与故障排查。

三种发布对象：

| 对象 | 怎么做 |
|---|---|
| 云端连接服务（`apps/cloud-control`） | 推送到 `main` 后由 GitHub Actions 自动部署；手动触发与手动部署见手册 §1 |
| 桌面端（`apps/desktop`） | `node scripts/release.mjs <版本号> "<说明>" --deploy` |
| 手机端（`apps/web/android`） | `node scripts/release-mobile.mjs <版本号> "<说明>" --deploy` |

三条铁律：

- **「发布」不等于授权提交。** 除非用户明确要求，不执行任何 git 操作（见上）。
- **服务器空间紧张，每个平台只保留最新一份安装包。** 发版脚本会自动清理历史版本，不要手工堆叠。
- **发布后必须回读线上清单与健康检查来验证**，不能只看脚本退出码；命令见手册 §4。

## Skills

- 只有用户在当前对话中明确指定某个 `superpowers` skill 时才能使用；不得根据任务类型自行启用任何 `superpowers` skill。
