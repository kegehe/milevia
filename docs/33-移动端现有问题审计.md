# 移动端现有问题审计（Android 远程控制端）

> 日期：2026-09-11
> 范围：`apps/web/android`（Capacitor 壳）、`apps/web/src/pages/MobileRemotePage.tsx`、`mobile-remote.css`、`scripts/release.mjs`
> **本文已逐条复核。初版有 2 条误判被撤回、1 条症状更正，均在第二节「核查更正」中列明依据。**

---

## 一、已核实成立

### 1. 发版脚本不写 Android 版本号（高）

- `scripts/release.mjs` 只同步三处桌面版本（`tauri.conf.json` / `Cargo.toml` / `desktop/package.json`，第 48–89 行）；第 52 行的 `androidDir` 仅用于 `cwd` 和取产物路径，脚本内**没有任何** `versionCode` / `versionName` 写入。
- 直接读已发布包的实证（`aapt2 dump badging release/Milevia_0.1.5_android.apk`）：

  ```
  package: name='info.keyanjia.milevia' versionCode='2' versionName='0.1.4'
  ```

- 真实影响（修正）：`versionCode` 相同且签名相同时**仍可覆盖安装**，所以"装不上"不成立。问题是：
  1. 包内版本（0.1.4）与对外文件名（0.1.5）不一致，任何读包内版本做判断的逻辑都会错；
  2. `versionCode` 不递增 → Google Play 不会接受为更新；
  3. 版本号只能靠手工改 `app/build.gradle` 维护，极易漏。
- 修法：与桌面三处一起递增 `versionCode`、写入 `versionName`。

### 2. 分发的是 debug 包（高）

- `release.mjs` 第 139 行执行 `gradlew.bat assembleDebug`，第 145 行取 `app/build/outputs/apk/debug/app-debug.apk`；`app/build/outputs` 下**只有** `apk/debug`，无 release 产物；`build.gradle` 无 `signingConfigs`，仓库内也没有任何 `.jks`/`.keystore`。
- APK 最终 Manifest 实证：`android:debuggable(0x0101000f)=true`。
- `release-notes.md` 第 49 行亦自述使用本机 debug 签名。
- 影响：不可上架；debuggable=true 有性能与安全代价；将来换正式 keystore 后，已装的 debug 版**无法覆盖升级**，必须卸载重装。

### 3. 通知能力在 APK 内不可用（高）

- APK 最终 Manifest 里的权限只有：`INTERNET`、`CAMERA`、`ACCESS_NETWORK_STATE`、`DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`——**没有 `POST_NOTIFICATIONS`**。
- `capacitor.plugins.json` 只注册 `@capacitor-mlkit/barcode-scanning` 与 `@capacitor/app`，没有任何推送/本地通知插件。
- MDN 兼容表明确标注：`Notification.permission` 在 **WebView Android = 不支持**（即 `window.Notification` 为 `undefined`）。
- 代码后果：`MobileRemotePage.tsx` 第 336 行 `typeof Notification === "undefined" → "unsupported"`；第 1483 行 `notificationPermission === "default" && <开启通知>` 永不渲染；第 760 行 `notifyHiddenMobileEvent()` 永远 return。
- 与设计文档《32-移动端App与云端远程控制终极方案》phase 4「推送通知」未落地一致，但 UI 预留了入口，容易误以为可用。

### 4. 物理返回键失效（高，症状已更正）

- 全项目无 `App.addListener("backButton")`；`CapacitorApp` 只用于 `minimizeApp()`（第 1474 行）。
- 原生包在 `App.tsx` 第 60/67 行整体替换为 `MobileRemotePage`，单路由、`canGoBack()` 恒为 false。
- Capacitor `AppPlugin.java` 第 46–62 行的默认实现：

  ```java
  this.onBackPressedCallback = new OnBackPressedCallback(!disableBackButtonHandler) {  // 默认 true
      public void handleOnBackPressed() {
          if (!hasListeners(EVENT_BACK_BUTTON)) {
              if (bridge.getWebView().canGoBack()) bridge.getWebView().goBack();
          } else { /* 派发给 JS */ }
      }
  };
  ```

- 结论（更正）：无监听 + 无法回退时，该回调**什么也不做但会吞掉事件**，因此返回键变成**「无操作」**——既不回项目列表，也不退出应用。（初版写成"会直接退出 App"是错的。）
- 修法：注册 `backButton`，按 `mobileView` / `tasksOpen` / 弹窗状态分层返回，最外层再 `minimizeApp()`。

### 5. 云端令牌存 localStorage，且随系统备份（中）

- `MobileRemotePage.tsx` 第 229 / 246 / 313 / 882 行读写 `localStorage.milevia.cloud.token`。
- APK 最终 Manifest 实证：`android:allowBackup(0x01010280)=true`。
- 影响：长时令牌未进系统安全存储，且会进入 adb / 云备份。设计文档 6.2 要求 Refresh Token 存系统安全存储（对应 OWASP MASVS），实现与该要求不符。

### 6. `custom_url_scheme` 只声明未接线（低，配置冗余）

- `res/values/strings.xml` 第 6 行声明了 `custom_url_scheme`。
- APK 最终 Manifest 中 `MainActivity` 的 intent-filter 只有 `MAIN` + `LAUNCHER`，**没有 `<data android:scheme=...>`**。
- 且 `@capacitor/android` 的 Java 源码里完全不引用 `custom_url_scheme`（Capacitor 不会自动接线，需手写 intent-filter）。
- 影响：仅「预留未启用」，当前没有依赖深链的功能，属清理项。

### 7. 页面头部没有顶部安全区处理（中，带前置条件）

- Capacitor 8 `SystemBars.java` 第 200 行：

  ```java
  boolean shouldPassthroughInsets = getWebViewMajorVersion() >= WEBVIEW_VERSION_WITH_SAFE_AREA_FIX /* 140 */ && hasViewportCover;
  ```

  第 206–223 行在该分支下**只给 WebView 加 IME 的底部 padding**，顶部完全铺开，仅注入 `--safe-area-inset-*` 并依赖 WebView 自己暴露 `env(safe-area-inset-*)`；默认 `insetsHandling = "css"`（第 37/58/99 行）。
- `index.html` 含 `viewport-fit=cover` → `hasViewportCover = true`。故 **WebView ≥ 140 时页面真正全屏铺开**。
- 而全项目 `env(safe-area-inset-top)` **只出现在一处**：`mobile-remote.css` 第 79 行 `.mobile-task-drawer`。页面 header 只有 `.mobile-remote { padding: 24px ... }`，≤620px 时降为 `18px 12px`（第 43 行）。
- 结论：在 WebView ≥ 140 的设备上，状态栏/挖孔会压住标题与品牌标识。WebView < 140 时 Capacitor 改走原生 padding 分支，不会重叠。
- 修法：header 加 `padding-top: max(18px, env(safe-area-inset-top))`。

### 8. 消息列表不自动滚动（中）

- 全文件 grep `scroll|scrollTop|scrollIntoView` 只命中第 1077 行的 `input.scrollHeight`（输入框自增高），**没有任何列表滚动控制**。
- `.mobile-message-list` 也没有做成独立的内部滚动容器，靠整页滚动。
- 影响：发出消息或 Agent 回复到达后，视口不跟随，用户必须手动下滑才能看到新内容。

### 9. 后台轮询不随页面可见性暂停（中，范围已收窄）

- 常驻不暂停的三项：第 796 行 `setInterval(loadInstances, 5000)`、第 811 行 `setInterval(loadSnapshot, 15_000)`、以及第 826 行起的 SSE 长连接（断线按 1s→30s 退避重连）。
- 全文件无 `visibilitychange` / `useDocumentVisible`（`lib/useDocumentVisible.ts` 存在但此处未用）。
- 更正：命令状态轮询（5s）、手机等待确认（1.5s）、桌面确认轮询（2s）都是**有条件的**，随操作结束而清理，不计入本条。

### 10. 触控目标偏小（低）

| 元素 | 有效高度 |
|---|---|
| `.mobile-task-filters button` | 30px |
| `.mobile-conversation-picker-trigger` | 40px |
| `.mobile-new-conversation` / `.mobile-task-toggle` | 40px |
| `.mobile-task-drawer > header > button` | 40px |
| `.mobile-task-modal header button` | 34px |

已达标：`.mobile-back` 44、`.mobile-refresh` 44、`.mobile-project-choice` 88、`.mobile-task-disclosure summary` 52（第 125 行覆盖了第 87 行的 32px，**不以 32px 计**）。

### 11. 大量 10–11px 字号（低）

`mobile-remote.css` 中 `font-size: 10px/11px` 共 **20 处**，集中在状态标签、meta 行、任务副标题、抽屉说明。

### 12. `cloudURL` 原生兜底硬编码（低）

`MobileRemotePage.tsx` 第 156 行：`Capacitor.isNativePlatform() ? configuredCloudURL || "https://keyanjia.info:8443"`，非标准端口。自建/内网/换域名都必须重打包 APK。

### 13. 已核实**不是**问题（避免重复排查）

| 事项 | 核实结果 |
|---|---|
| 云端证书 | `curl https://keyanjia.info:8443/health` → HTTP 200，`ssl_verify_result=0`（证书链校验通过），非自签 |
| 启动页主题 | `AppTheme.NoActionBarLaunch parent="Theme.SplashScreen"` + `android:background` 就是 Capacitor 官方模板写法 |
| Service Worker | 仅在 http(s) 源注册；`sw.js` fetch 白名单已排除 `/api/`、`/v1/`、`/ws/`，SSE 不受缓存影响 |
| 原生分支平台判定 | `App.tsx` 绕过 Provider 树直接渲染移动页，不会误开本地 API/WebSocket，设计正确 |
| 扫码链路 | MLKit 依赖 meta-data、CAMERA 权限、权限拒绝文案均已处理 |
| 指令幂等与乱序 | `mergeCommandState()` 有终态保护；测试覆盖了快照 revision 收敛与消息复活问题 |

---

## 二、核查更正（初版误判，已撤回）

### ❌ 撤回：`colors.xml` 缺失导致构建失败

**实际不存在此问题。** 跑 `:app:processDebugResources --rerun-tasks`（强制重新 merge 资源）结果为 **BUILD SUCCESSFUL in 57s**。

那三个颜色的真实来源是 Capacitor 自身的库：

```
node_modules/.pnpm/@capacitor+android@8.5.1_@capacitor+core@8.5.1/
  node_modules/@capacitor/android/capacitor/src/main/res/values/colors.xml
    <color tools:ignore="UnusedResources" name="colorPrimary">#3F51B5</color>
    <color tools:ignore="UnusedResources" name="colorPrimaryDark">#303F9F</color>
    <color tools:ignore="UnusedResources" name="colorAccent">#FF4081</color>
```

我判错的原因：`node_modules/@capacitor/...` 是 pnpm 创建的**符号链接**，前两次 grep 没有跟进 `.pnpm` 实体目录，于是误以为「仓库和依赖里都没有」。

附带更正：那三个色确实是模板默认靛蓝，但使用它的 `AppTheme`（parent `Theme.AppCompat.Light.DarkActionBar`）并不在 Activity 实际主题链上（Activity 用 `AppTheme.NoActionBarLaunch` → `Theme.SplashScreen`），所以**没有可见影响**，只是主题配置里的一段冗余。

### ❌ 撤回：http 二维码与 Android 明文策略冲突

**实际不影响配对。** 手机从不加载二维码里的 URL：`acceptScannedPairing()` → `pairingFromScan()` 只用 `new URL(value)` 取 `pairingId` / `code` 两个查询参数；随后 `claimPairing()` 调的是 `cloud()` → `${cloudURL}/v1/pairings/...`（`cloudURL` 固定为 `https://keyanjia.info:8443`）。所以 QR 用 http 还是 https 与手机能否连上无关。`pairingURLWithCode()` 放行 `http:` 只是一段用不到的宽松判断，属代码整洁性问题。

---

## 三、待真机确认（无法从代码定论）

1. **第 7 条顶部安全区**需在 WebView ≥ 140 的真机上确认重叠程度（取决于状态栏高度与是否挖孔）。
2. **PWA 图标**：`public/manifest.webmanifest` 只声明了 1 个 `sizes:"any"` 的 SVG。web.dev 官方说明要求 Chromium 至少提供 192×192 与 512×512 的图标，并建议「为不支持 SVG 的浏览器提供光栅兜底」。当前声明不满足该建议，**是否影响「添加到主屏幕」横幅需在目标浏览器实测**，不宜直接判定为缺陷。
3. **键盘弹出**：Manifest 已有 `adjustResize`，且 `SystemBars.java` 第 208 行在键盘弹出时会补底部 padding，基础可用；无 `visualViewport` 兜底在个别机型上可能跳动，需实测。
4. **横屏**：`MainActivity` 未锁定方向，横屏下会落到 820px 档位布局，未验证。

---

## 四、建议处理顺序

1. **第 1、2 条**（版本号同步 + release 签名），交付阻塞项，改动小。
2. **第 3、4 条**（通知能力、返回键），直接影响「手机当任务控制台」的核心体验。
3. **第 5、7、8 条**（令牌存储、顶部安全区、自动滚动）。
4. **第 9–12 条**与第三节的真机项，随实测收敛。

补充：`apps/web/android/app/src/androidTest` 目前只有 Capacitor 模板生成的空测试，建议补一份真机冒烟清单（安装 → 扫码配对 → 进会话 → 发消息 → 收回复 → 返回键 → 键盘 → 断网重连 → 卸载重装），上述问题大半会在该清单里暴露。

---

## 五、核查方法

| 结论 | 依据 |
|---|---|
| 包内版本号 | `aapt2 dump badging release/Milevia_0.1.5_android.apk` |
| debug 签名 / 权限 / 备份 / 深链 | `aapt2 dump xmltree --file AndroidManifest.xml`（读**已发布 APK 的最终合并 Manifest**，比读源码 Manifest 更权威） |
| 返回键行为 | `@capacitor/app` 的 `AppPlugin.java` 源码 |
| 安全区行为 | `@capacitor/android` 的 `SystemBars.java` 源码 |
| WebView 通知支持 | MDN `Notification.permission` 兼容表（WebView Android = 不支持） |
| 资源缺失问题 | `gradlew :app:processDebugResources --rerun-tasks` 实跑 |
| colors 来源 | 全量搜索 `.pnpm` 实体目录（**不能**只搜符号链接路径） |
| 前端行为 | 对 `MobileRemotePage.tsx` / `mobile-remote.css` 逐项 grep 复核 |

> 环境提示：本机 Git Bash 的 PATH 被 shim 破坏（`ls`/`head`/`tail`/`cat` 均不可用），涉及管道与文件列举的核查一律改用 PowerShell / Read / Grep / Glob。Gradle 构建需写入 `app/build/intermediates`，在沙箱内会被拒绝，需在沙箱外执行。

---

## 六、修复记录（2026-09-11）

### 已修

| # | 问题 | 改动 |
|---|---|---|
| 1 | 发版脚本不写 Android 版本号 | `scripts/release.mjs` 新增「1.5 Android 版本号」：解析并递增 `versionCode`、写入 `versionName`；同版本重跑不重复递增（幂等） |
| 2 | 分发 debug 包 | `app/build.gradle` 新增 `signingConfigs.release`（优先 `android/keystore.properties`，其次 `MILEVIA_ANDROID_KEYSTORE*` 环境变量），`buildTypes.release` 按配置选择签名；无配置时回退 debug 并 `logger.warn` 告警。`release.mjs` 相应改用 `assembleRelease` / `assembleDebug`。`android/.gitignore` 取消注释并补 `keystore.properties`，防止签名材料入库 |
| 4 | 物理返回键失效 | `MobileRemotePage.tsx` 注册 `App.addListener("backButton")`，按「弹窗 → 会话菜单 → 扫码 → 任务抽屉 → 配对面板 → 会话视图 → 最小化应用」逐层回退；用 `backHandlerRef` 保证监听只注册一次也能取到最新状态 |
| 5 | 令牌随系统备份外泄 | `AndroidManifest.xml` 的 `allowBackup` 改为 `false`（业务数据都在云端/电脑本地，关掉不丢用户数据） |
| 7 | 顶部无安全区 | `mobile-remote.css` 的 `.mobile-remote` 与 `.mobile-conversation-mode` 顶部 padding 改为 `calc(Npx + env(safe-area-inset-top))` |
| 8 | 消息列表不自动滚动 | 新增 `stickToBottomRef` + window scroll 监听（160px 容差）与跟随效果；`stickToBottomRef` 在进入项目时重置 |
| 9 | 后台轮询不暂停 | 接入既有 `useDocumentVisible()`：隐藏时停掉 5s 实例轮询与 15s 快照兜底（SSE 保留），回到前台立即补刷实例与快照 |
| 10 | 触控目标偏小 | 筛选 chip 30→44px、会话选择器 / 新会话 / 任务按钮 40→44px、抽屉与弹窗关闭键 34–40→44px、扫码取消补 44px |
| 11 | 字号偏小 | `mobile-remote.css` 中 20 处 10–11px 统一提到 12px |

### 代码复查后追加的加固（同日第二轮）

自查改动时发现并修掉的隐患：

| 隐患 | 加固 |
|---|---|
| 流式回复只改 content、不改消息条数，「贴底跟随」在 AI 输出时不会生效 | 增加 `lastMessageLength` 依赖，跟随流式内容 |
| 弹层提交中按返回键会关掉弹层，而弹层内「关闭」按钮此时是 disabled —— 行为不一致 | 返回键增加 `busy && (弹层开启)` 时吞掉事件的守卫 |
| 回前台补刷挂在 `[documentVisible, instanceID, ...]` 上，切换电脑时会多拉一次完整快照 | 用 `wasDocumentVisibleRef` 收紧为只在「隐藏 → 可见」瞬间触发 |
| CSS 里 `calc(24px + env(safe-area-inset-top))` 若浏览器不支持 `env()`，**整条 padding 声明会被丢弃**，内容会贴边 | 改为「先写普通 padding，再写 calc 版本」的渐进增强写法（3 处） |
| `keystore.properties` 存在但字段不全时会**静默回退成 debug 签名**，产出一个看着像正式包、其实不能上架也不能覆盖安装的 APK | `build.gradle` 增加字段完整性与签名文件存在性校验，不满足直接 `throw GradleException` |
| 上述校验最初放在 `android {}` 之前，报错时会连带抛出误导性的 `does not specify compileSdk` 次生错误 | 校验移入 `android {}` 内、`compileSdk` 赋值之后；复测确认次生错误消失 |
| `release.mjs` 的 Android 版本块重复 `readFileSync`，且缺包时报错信息不够可用 | 改为只读一次；缺包时报错会说明当前选择的 build type 与 `--no-build` 的影响 |

### 第三轮复查（换角度：查改动与其它代码的交互缺口）

| 隐患 | 加固 |
|---|---|
| **「后台不拉快照」被 SSE 路径抵消**：SSE 事件到达时会调 `scheduleSnapshotRefresh()`，页面隐藏时它照样每 0.5–1s 拉一份完整快照 —— 前面暂停轮询的优化实际无效 | `scheduleSnapshotRefresh` 的定时回调里加可见性判断；回前台由可见性效果整体补刷一次 |
| **`env()` 无兜底的地方没修完**：`.mobile-composer { bottom: env(...) }` 在不支持 `env()` 时整条 `bottom` 被丢弃 → 退回 `auto`，固定定位的输入框直接错位（比顶部贴边更严重） | 该处及任务抽屉 padding、会话模式 `padding-bottom` 共 4 处补上回退声明。现全表 7 处 `env()` 均有回退 |
| **键盘弹出会把「贴底」判定顶掉**：`adjustResize` 缩小视口后 `scrollHeight - scrollY - innerHeight` 会多出键盘高度，导致发送后不跟随 | 在 `sendConversationMessage()` 里无条件 `stickToBottomRef.current = true`（发送即表示想看到新内容） |
| **贴底容差 160px 偏大**：手机上约 1/5 屏，用户轻微上滑阅读会被拽回 | 收到 100px（仍足以吸收地址栏收放的视口变化） |
| **`release.mjs` 的正则没锚定行首**：若 `build.gradle` 里出现注释形式的历史版本（如 `// versionCode 2`，调试时很常见），会被当成真值 —— 实测旧写法会把 `versionCode` 写成 999 | 改为 `/^[ \t]*versionCode\s+(\d+)/m` 锚定行首；已用「插入诱饵注释」实测确认不再被误导 |

### 验证

- `pnpm --filter @milevia/web test` → **214 pass / 0 fail**（第三轮改动后复跑仍为 214/0）
- `pnpm --filter @milevia/web exec tsc --noEmit` → **exit 0**
- `pnpm --filter @milevia/web build`（tsc -b + vite build，覆盖 CSS）→ **exit 0**，`built in 1.24s`
- CSS 结构校验（postcss 解析 `mobile-remote.css`）→ **PARSE_OK**：242 条规则 / 6 个 at-rule / 886 条声明，残留 10–11px 字号 **0 处**
- `gradlew :app:assembleRelease` → **BUILD SUCCESSFUL**，产物 `app/build/outputs/apk/release/app-release.apk`：
  - `aapt2 dump badging` → `versionCode='3' versionName='0.1.5'`（版本同步生效）
  - `aapt2 dump xmltree` → `allowBackup=false`，**无 `debuggable`**（release 构建类型生效）
  - 日志出现预期的签名回退告警（当前未配置 keystore），且**无** `does not specify compileSdk` 次生错误
- `node scripts/release.mjs --bump-only 0.1.5` 连跑多次 → 始终 `versionCode 3 → 3`，桌面三个版本文件未被改动
- 结构检查：新增的 8 个标识符均单一声明、无重复定义、无死代码
- 签名校验两条错误分支实测（用完即删临时配置，已确认目录干净）：
  - 字段不全 → `Android 正式签名配置不完整，缺少：storePassword, keyAlias, keyPassword。…`
  - 文件不存在 → `Android 签名文件不存在：…/missing.jks`
  - 两者均**不再**连带出现 `does not specify compileSdk`

### 如何配置正式签名（上架/覆盖安装前必须做）

1. 生成 keystore（长期保管，不要提交到 Git）：

   ```powershell
   keytool -genkeypair -v -keystore release.jks -alias milevia -keyalg RSA -keysize 4096 -validity 10000
   ```

2. 在 `apps/web/android/keystore.properties` 写入（该文件已被 gitignore）：

   ```properties
   storeFile=/abs/path/to/release.jks
   storePassword=***
   keyAlias=milevia
   keyPassword=***
   ```

   或改用环境变量：`MILEVIA_ANDROID_KEYSTORE` / `MILEVIA_ANDROID_KEYSTORE_PASSWORD` / `MILEVIA_ANDROID_KEY_ALIAS` / `MILEVIA_ANDROID_KEY_PASSWORD`。

3. 之后 `node scripts/release.mjs <version>` 会自动改跑 `assembleRelease` 并输出正式签名包；日志不再出现回退告警。

> 注意：已装出去的 0.1.5（debug 签名）与将来的正式签名包签名不同，**无法互相覆盖安装**，切换时必须先卸载旧包。

### 未修（需你决定）

- **推送通知**：Android WebView 不支持 `Notification`，APK 内无法做后台通知。要真正可用需接 FCM（新建 Firebase 项目 + `google-services.json`）或 `@capacitor/local-notifications`，涉及外部账号与原生依赖，待你确认后再做。
- **令牌安全存储**：目前仍是 `localStorage`。改用系统安全存储需要引入第三方插件（如 `capacitor-secure-storage-plugin`）或自写原生插件，涉及新增依赖，待你确认。
- **云端地址可配置**：`cloudURL` 原生兜底仍硬编码 `https://keyanjia.info:8443`。做成可配置需要在 App 内加「服务器地址」输入与持久化，属功能新增。
- **`custom_url_scheme` 冗余声明**：仅字符串声明、无 intent-filter，当前无功能影响，未动它以免引入无关改动。
